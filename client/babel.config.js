module.exports = {
    presets: [
        '@babel/preset-env'  // Đảm bảo hỗ trợ các tính năng mới của JavaScript
    ],
    plugins: [
        '@babel/plugin-proposal-optional-chaining'  // Cung cấp hỗ trợ cho optional chaining
    ]
};